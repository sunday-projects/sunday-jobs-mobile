import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  defaultContainer: {
    backgroundColor: '#fff',
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1
  },
  centerOnly: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1
  }
});
